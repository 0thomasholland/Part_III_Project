\subsection{Bayseian Inversion}

% Breif introduction

By applying the inversion framework outlined in \autoref{sec:bay}, which is implemented within the \citetitle{al-attarPygeoinfPythonLibrary2025}, we can implement the physics outlined previously with the prior distributions built to reflect our prior belief of our knowledge of the system to get result in our posterior belief of the state of the model space.



\subsubsection{Simple inversion}

\notebookcite{thomasholland06SimpleInversionipynb2026}

A simple case for this is setting the model space to be a rotationally invariant ice thickness change with a single density and the data space to be changes in SSH altimetry, . This would result in the following form:

\begin{align}
	d &= \operatoraltimetry{\text{ocean}} \circ \operatorsshc \circ \operatorslc \circ \operatorloadice (\thicknesschange{i}) + n\\
	d &= \operator{O}{simple}(\thicknesschange{i}) + n \label{eq:simple_case}
\end{align}

Where $\thicknesschange{i}$ is the ice thickness model space and $n$ represents the altimetry noise, which is taken to be an independent Gaussian with zero mean and covariance $\covariance{n}$, which represents the uncertainty in the altimetry observations:

\begin{equation}
	n \sim \mathcal{N}(0, \covariance{n})
\end{equation}

A true ice-thickness field, $\thicknesschange{i}^\dagger$,  is drawn as a single sample from the prior measure $\thicknesschange{i}$, providing a plausible but non-trivial test case that is, by construction, consistent with our prior assumptions. Synthetic altimetry observations, $d_{obs}$, are then generated by passing through the forward operator, \autoref{eq:simple_case} and adding Gaussian noise:

\begin{equation}
	d_{obs} = \operator{O}{simple}(\thicknesschange{i}^\dagger) + n
\end{equation}

Using the Bayseian inversion framework, we are able to generate the posterior model space parameters (?!?), $m_{\Delta I}^{p}$, which can be compared to the used to inspect the new inversion method. Such inspections can include simple visual inspection of the posterior model expectation, which captures ???, with the true field. Both of these can be pushed forward through the sea level operator to obtain their respective sea level fingerprints, also propagating their uncertainty.

\subsubsection{Comparison with previous method}
\notebookcite{thomasholland06SimpleInversionipynb2026}

We collapse the full spatial sea-level fingerprints to a single scalar: the global mean sea level (GMSL) contribution. This is accomplished by applying the GMSL projection operator, a spatially uniform averaging kernel, appropriately area-weighted, to both the posterior distribution and the true field. This generates a true GMSL value, $\Delta GMSL^{true}$, and the inversion method's estimate, $\Delta GMSL^{bay}$:

\begin{align}
	\Delta GMSL^{true} &= G m_{\Delta I}^\dagger \label{eq:true-gmsl-model} \\
	\Delta GMSL^{bay} &= G m_{\Delta I}^{p} \label{eq:bay-gmsl-model}
\end{align}

For comparison, I also computed an old method GMSL estimate obtained by treating the raw altimetry observations as a direct proxy for sea-level change, without any inversion step, as described in \autoref{}. This method propergates through the altimetry error used in our inversion, $n$, with the assumption that other sources of error are consistent to both methods, for example the errors assocaited with data cleaning steps, and thus moot.

\begin{equation}
  \Delta GMSL^{alt} \sim \mathcal{N}\left(\dfrac{1}{\mathbf{Len}[d_{obs}]}\sum^{\mathbf{Len}[d_{obs}]}_{i=1} d_{obs,\,i},\, \mathcal{C}_n\right) \label{eq:alt-gmsl-model}
\end{equation}

The $z$-score can be calculated for each method:

\begin{align}
	z^{bay} &= \dfrac{\Delta GMSL^{true} - \mathbb{E}[\Delta GMSL^{bay}]}{\mathbb{SD}[\Delta GMSL^{bay}]} \\
	z^{alt} &= \dfrac{\Delta GMSL^{true} - \mathbb{E}[\Delta GMSL^{alt}]}{\mathbb{SD}[\Delta GMSL^{alt}]}
\end{align}

The inversions can be run multiple times to generate a distribution of $z$-scores for each method, which can be compared to show the improvement of the new method over the old method.

\subsubsection{Sensitivity tests}
\notebookcite{thomasholland07InversionSensitivityipynb2026}

Using similar methods to those described in \textcite{heathcoteScaleableBayesianFramework2026} for testing the inversion sensitivity to changing parameters.

Intially, an inversion scenario is set up similar to the simple inversion, with a fixed truth ice state, $m_{\Delta I}^\dagger$. This allows for the altimetry estimation measure, $\Delta GMSL^{alt}$ (see \autoref{eq:alt-gmsl-model}), to be formed upfront as a comparison metric alongside the true GMSL change value, $\Delta GMSL^{true}$ (see \autoref{eq:true-gmsl-model}). Inversions of the true field were run with priors generated with systematically varied parameters of interest.

To test for the sensitivity of the ice length scale prior model, various length scales to use for the prior were selected around the true field, which was generated at 10\% of the mean sea floor radius $\approx 636.8$.

To test for the sensitivity of offsetting the prior by a fixed amount, equivilent to adding a uniform thickness of across the ice field, before using this to invert to.
Generated with 10\,mm standard deviation and had a mean of 0.3764 mm.






In addition to the above tests from \textcite{heathcoteScaleableBayesianFramework2026}, I tested the sensitivity of the method to the ice thickness prior model to spatial patters or not. I generated true ice states for both rotationally invariant ice change patterns and thickness dependent change patters (see \autoref{sec:ice_thickness_priors}). These were both inverted using the two types of prior model, and then compared.

\subsubsection{Joint inversion using ice and firn thickness}



\subsubsection{Joint inversion using ice, firn, and ocean dynamic changes}
% Paragraph: Describe the joint inversion -- ice + firn + ocean dynamics
% inverted simultaneously. Explain why this matters (correlated signals).

The model space would be a direct sum of the ice thickness change, firn thickness change space, and ocean dynamic topography change:

\begin{equation}
	m = m_{\Delta I} \oplus m_{\Delta F} \oplus m_{\Delta ODT}
\end{equation}

With the data space being the direct sum of SSH altimetry data, tide gauge data, and ice altimetry data:

\begin{equation}
	d = d_{\Delta SSH} \oplus d_{\Delta TG} \oplus d_{\Delta ISH}
\end{equation}

It is often the case where multiple model space parameters map to one data space parameter, for example in our case, SSH altimetry points forms the linear equation:

\begin{equation}
	d_{\Delta SSH} = P_{S}( SSH \circ F (L_I m_{\Delta I} + L_F m_{\Delta F} + L_W m_{\Delta ODT}) + m_{\Delta ODT})
\end{equation}

The forward operator for the joint inversion can be written as a block matrix, with each block being the mapping of the respective model space parameter to the respective data space parameter. This results in the following form:

\begin{align}
\begin{bmatrix}
	\text{SSH Altimetry Points}\\
	\text{Tide Gauge Points}\\
	\text{Ice Altimetry Points}
\end{bmatrix}
&\nonumber\\&=
\begin{bmatrix}
	\mathbf{P_S S F L_I} & \mathbf{P_S S F L_F} & \mathbf{P_S(S F L_W + 1)} \\
	\mathbf{P_T F L_I} & \mathbf{P_T F L_I} & \mathbf{P_T (F L_W + 1)}\\
	\mathbf{P_I} & \mathbf{P_I} & \mathbf{0}
\end{bmatrix}
\begin{bmatrix}
	\Delta T_\text{Ice}\\
	\Delta T_\text{Firn}\\
	\Delta SSH_\text{OD}
\end{bmatrix}
\end{align}

However, this is computationally inefficient, as it requires six independent calls to the fingerprint numerical solver. To optimise this forward operator to reduce the number of fingerprint calls, we can factorise this matrix into sparse matrices, shown in \autoref{eq:factorised_joint}. This reduced each iteration time, with a speed-up of $3\times$.

\begin{equation}
=
	\begin{bmatrix}
\mathbf{P_S S} & \mathbf{P_S} & 0 & 0 \\
\mathbf{P_T} & \mathbf{P_T} & 0 & 0 \\
0 & 0 & \mathbf{P_I} & \mathbf{P_I}
\end{bmatrix}
\begin{bmatrix}
\mathbf{F} & 0 & 0 & 0 \\
0 & \mathbf{1} & 0 & 0 \\
0 & 0 & \mathbf{1} & 0 \\
0 & 0 & 0 & \mathbf{1}
\end{bmatrix}
\begin{bmatrix}
\mathbf{L_I} & \mathbf{L_F} & \mathbf{L_W} \\
0 & 0 & \mathbf{1} \\
\mathbf{1} & 0 & 0 \\
0 & \mathbf{1} & 0
\end{bmatrix}
\begin{bmatrix}
\Delta T_{\text{Ice}} \\
\Delta T_{\text{Firn}} \\
\Delta SSH_{\text{OD}}
\end{bmatrix}\label{eq:factorised_joint}
\end{equation}

% Paragraph: Discuss preconditioning and why it is needed for numerical
% efficiency when the operator is ill-conditioned.


% Paragraph: Describe application to altimetry data and/or DUACS SSH data.

\subsubsection{Sensitivity tests of joint inversions}

TKTK covariance tests

TKTK knock out tests

\subsubsection{Proof of concept with data}

As a showcase of this method and proof of concept, I utilised the CITE to subsample the ocean, which with sensible
