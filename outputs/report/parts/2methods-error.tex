\subsection{Error Quantification}

The objective of this error quantification to interrogate how varying model parameters affect then error of the old method, and how these vary.

In estimating the error associated with using sea surface height change as a proxy for global mean sea level change, we adopt a best-case assumption of dense, uniform sampling  across the altimetry domain $\Omega$. In this limit, the discrete sum over altimetry measurement points converges to a continuous surface integral so that the estimated global mean sea level reduces to the true spatial average of the field:

\begin{equation}
    \frac{1}{|\Omega|}\sum_{i=1}^{N} \eta(\mathbf{x}_i)\, \delta A_i
    \;\xrightarrow{N \to \infty}\;
    \frac{1}{|\Omega|}\int_{\Omega} \eta(\mathbf{x})\, \mathrm{d}S(\mathbf{x}).\label{eq:altimetry_equation}
\end{equation}

This constitutes a best-case scenario for the estimation error: any realistic altimetry network, with finite and non-uniform spatial coverage, will perform no better than this ideal limit. By framing the error analysis in terms of this continuous average rather than a specific satellite configuration, the resulting error bounds are independent of the particular sampling geometry and reflect a fundamental lower bound on the approximation error inherent to the method itself.

Practically, working directly with the surface integral also avoids the need to generate and store a large list of altimetry sample points, reducing computational overhead and making the error estimation tractable for global-scale calculations.


To demonstrate and confirm this, a test was run to see how the effects of changing the altimetry resolution. Resolutions within the range of those needed for inversions (2 to 20 deg grid spacing) to resolutions typically provided by altimetry measures (5 to 0.25 deg grid spacing) were tested and compared against whole field SSH averages.

\notebookcite{thomasholland05AltimetrySamplingipynb2026}

To first demonstrate the systematic error introduced by using SSHC as a proxy for SLC when estimating GMSL from satellite altimetry. A synthetic ice sheet change scenario is constructed as a Gaussian measure over ice thickness, propagated through the sea level fingerprint operator to yield both the true SLC field and its corresponding SSHC field. GMSL is then estimated by forming the area-weighted average of SSHC over the altimetry-observable ocean (latitudes within ±66°), and compared against the true GMSL obtained from the area-weighted average of SLC.

\notebookcite{thomasholland01SeaSurface2026}

One of the objectives is to extend and cross validate this framework's result with that from \textcite{lickleyBiasEstimatesGlobal2018} demonstrating the error between SLC and SSHC. As such, I tested how altimetry error changes with altimetry latitude range. This is important as the altimetry error is expected to be higher at higher latitudes due to the increased variability in sea surface height and the presence of ice sheets which, when there is a change in mass causes a rebound effect, increasing the difference in SSHC and SLC.

To further investigate the impact of ice sheet variability on altimetry error, I also tested how the error changes when the ice melt is restricted to the major ice sheets (GIS, WAIS, and EAIS). This was done with a uniform melt across each ice sheet, normalised to result in 1 mm of true GMSL change, so that the percentage error using SSHC altimetry could be trivially calculated.

\notebookcite{thomasholland02MajorSourcesipynb2026}
